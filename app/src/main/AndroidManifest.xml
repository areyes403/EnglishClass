<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.itm.link.englishclass">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".presenter.MyApp"
        android:theme="@style/Theme.EnglishClass"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".presenter.activities.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".presenter.activities.LoggedActivity"
            android:exported="false" />
        <activity
            android:name=".presenter.activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".presenter.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>